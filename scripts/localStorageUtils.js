// Функция для удаления элемента из localStorage по ключу
function removeItemFromLocalStorage(key) {
    localStorage.removeItem(key);
}

// Функция для очистки всех данных из localStorage
function clearLocalStorage() {
    localStorage.clear();
}